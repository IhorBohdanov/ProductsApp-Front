import React from "react";
import { CategoryForm, Page } from "../components";

export const CreateCategory = () => {
  return (
    <Page pageTitle={'Create category'}>
      <CategoryForm />
    </Page>
  )
}